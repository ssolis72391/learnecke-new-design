import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  signUpLink: {
    fontWeight: 'bold',
    cursor: 'pointer',
    textDecoration: 'none',
  },
}));

export default useStyles;
